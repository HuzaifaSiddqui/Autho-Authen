<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Session Checking</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #393b3f;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            text-align: center;
            z-index: 1;
        }
        h1 {
            margin-bottom: 20px;
        }
        button {
            width: 100%;
            padding: 10px;
            background-color: #dc3545;
            border: none;
            border-radius: 4px;
            color: white;
            font-size: 16px;
            cursor: pointer;
        }
        button:hover {
            background-color: #c82333;
        }
        .floating-keys {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 0;
        }
        .key {
            position: absolute;
            font-size: 40px;
            color: white;
            animation: float 10s linear infinite;
        }
        @keyframes float {
            0% {
                transform: translateY(100vh) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(-100vh) rotate(360deg);
                opacity: 0;
            }
        }
    </style>
</head>
<body>
    <div class="floating-keys">
        <div class="key" style="left: 10%; animation-delay: 0s;">S</div>
        <div class="key" style="left: 20%; animation-delay: 2s;">E</div>
        <div class="key" style="left: 30%; animation-delay: 4s;">S</div>
        <div class="key" style="left: 40%; animation-delay: 6s;">S</div>
        <div class="key" style="left: 50%; animation-delay: 8s;">I</div>
        <div class="key" style="left: 60%; animation-delay: 10s;">O</div>
        <div class="key" style="left: 70%; animation-delay: 12s;">N</div>
    </div>
    <div class="container">
        <h1>Logout to remove the SessionID</h1>
        <form action="/logout" method="post">
            <button type="submit">Logout</button>
        </form>
    </div>
</body>
</html>